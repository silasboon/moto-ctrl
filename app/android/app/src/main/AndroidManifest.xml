<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- BLE scan/connect, used by the react-native-ble-plx transport.
         Android 12+ (API 31+) uses BLUETOOTH_SCAN/BLUETOOTH_CONNECT;
         neverForLocation is safe here since the app only scans for a
         specific device name and never derives location from scan
         results. Pre-12 devices (minSdkVersion 24) fall back to the
         legacy BLUETOOTH/BLUETOOTH_ADMIN + location permissions, which
         Android required for BLE scanning before API 31. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- BleWatchService: a foreground service so the process survives
         backgrounding long enough for phone-as-key to reconnect without the
         app open (Android 8+ kills/freezes backgrounded apps with no
         foreground service; Doze makes it worse). FOREGROUND_SERVICE is the
         general grant every foreground service needs;
         FOREGROUND_SERVICE_CONNECTED_DEVICE is the specific one Android 14+
         (API 34) requires for a service whose type is "connectedDevice" — see
         the <service> entry below. POST_NOTIFICATIONS (Android 13+) is what
         actually lets the service's persistent notification show; the
         service itself still runs without it, just silently. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- No android:roundIcon: the ic_launcher_round set was dropped when the
         launcher icons were replaced with an adaptive set (background +
         foreground + monochrome), and the leftover reference failed resource
         linking on the next clean build.

         Removed rather than restored, deliberately. The adaptive icon in
         mipmap-anydpi-v26 covers API 26+, where the launcher applies its own
         mask — round, squircle, teardrop — so a hand-made round variant is
         redundant there. On API 24-25 Android falls back to android:icon,
         which is all roundIcon was ever a Nougat-era refinement of. Bringing
         back the old round PNGs would have shipped the previous icon art
         alongside the new. -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <!-- Started/stopped from JS (src/ble/nativeBleWatchService.ts) as
           BoardSession's watch/connect/connected state changes. -->
      <service
        android:name=".ble.BleWatchService"
        android:exported="false"
        android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
