#pragma once

/* See mc_ota_release_key.c for the full doc comment (placeholder status,
 * key rotation, how the maintainer replaces this before a real release). */

#include "mc_crypto.h"

extern const uint8_t MC_OTA_RELEASE_PUBKEY[MC_CRYPTO_PUBKEY_BYTES];
